# State files — contain everything in plaintext
*.tfstate
*.tfstate.*

# Your actual secret values
*.tfvars
*.tfvars.json

# Provider binaries — huge, regenerated by terraform init
.terraform/

# Lock file is fine to commit, so no entry for .terraform.lock.hcl

# SSH keys if they live in the project folder
*.pem
id_rsa*